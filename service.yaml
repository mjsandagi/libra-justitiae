apiVersion: v1
kind: Service
metadata:
    name: libra-service
spec:
    selector:
        app: libra-app
    ports:
        - protocol: TCP
          port: 80 # Port accessible inside the cluster
          targetPort: 5000 # Port the container is listening on
    type: LoadBalancer # Or NodePort if using Minikube default
